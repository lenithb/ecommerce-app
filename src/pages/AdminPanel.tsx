// panel de administración (ruta protegida).
//   import { useContext } from "react"                 -> AuthContext, ProductContext
//   agregar/eliminar productos usando las funciones del contexto, no lo pongo porque empiezan a saltar errores

export function AdminPanel() {
  return <main>AdminPanel</main>;
}
