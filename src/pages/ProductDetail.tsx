// detalle de un producto (ruta /product/:id).
//
//   import { useParams } from "react-router-dom"       -> leer el id
//   import { useProduct } from "../hooks/useProduct"   -> para tener el producto

export function ProductDetail() {
  return <main>ProductDetail</main>;
}
