// formulario de login.
//
//   import { useState } from "react"                 -> email / password
//   import { useContext } from "react"               -> AuthContext (login)
//   import { useNavigate } from "react-router-dom"   -> redirigir al tener sesión

export function Login() {
  return <main>Login</main>;
}
